This project calls DumpDependencies to generate a report of web dependencies used in 
stardust's web portal. Run by calling "mvn clean package". It depends on Stardust Artifacts 
being available with same version as the project's version. 